# @wingman/schema

Runtime-validated, dependency-light contracts shared by `@wingman/sdk` and a
Wingman control plane. Application integrations should normally install
`@wingman/sdk`; this package is public so wire formats remain inspectable and
versioned.

Licensed under MIT.
